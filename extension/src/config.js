/* Baked-in TMS Firebase config (public web config — safe to ship in the client;
   security is enforced by Firebase Auth + Firestore rules). Dispatchers never
   enter this — the extension just works after they sign in once. */
export const FIREBASE = {
  apiKey: "AIzaSyC05k--mNg815IP4SO0As5KRgQVFOlDlJg",
  projectId: "rfa-tms-42a31",
};
