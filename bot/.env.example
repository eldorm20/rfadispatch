# Copy to bot/.env and fill in. bot/.env is gitignored — never commit secrets.
TELEGRAM_BOT_TOKEN=123456:ABC-your-bot-token
FIREBASE_PROJECT_ID=rfa-tms-42a31
# Path to a Firebase service-account JSON (Console → Project settings → Service accounts → Generate new private key)
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json
